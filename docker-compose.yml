services:
  vh-payment-bo:
    container_name: vh-payment-bo
    image: "${IMAGE_NAME}"
    restart: unless-stopped
    cpus: '0.1'
    mem_limit: 50M
    ports:
      - 8087:80
    volumes:
      - /opt/vh/vh-payment-bo/public/config/:/usr/share/nginx/html/config/
networks:
  default:
    name: vh
    external: true
