// Auto-loaded by react-scripts before each test file runs.

// App modules read runtime config from window.APP_CONFIG at import time; provide
// a stub so importing them under jsdom doesn't throw.
window.APP_CONFIG = window.APP_CONFIG || { VH_API_BASE_URL: "http://localhost" };
